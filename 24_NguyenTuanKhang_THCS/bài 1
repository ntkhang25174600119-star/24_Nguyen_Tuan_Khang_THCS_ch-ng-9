# nhập dữ liệu từ bàn phím 
gia = float(input("Nhập giá sản phẩm: "))
so_luong = int(input("Nhập giá số lượng mua : "))

# tính tổng chi phí chưa thuế 
tong_chi_phi = gia * so_luong 

# tính thuế VAT (10%)
thue_vat = tong_chi_phi * 0.10 

# tính tổng tiền phải trả
tong_tien = tong_chi_phi + thue_vat 

# in kết quả (làm tròn đến 2 chữ số tphan)
print(f"tổng chi phí chưa thuế: {tong_chi_phi:.2f}")
print(f"thuế VAT (10%): {thue_vat:.2f}")
print(f"tổng tiền phải trả: {tong_tien:.2f}")
